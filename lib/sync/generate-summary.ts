/**
 * Re-export from the canonical implementation.
 * The sync engine imports from here; the full logic lives in lib/generate-thread-summary.ts.
 */
export { generateThreadSummary } from "@/lib/generate-thread-summary";
